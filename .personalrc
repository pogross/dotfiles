# poetry
export PATH="$HOME/.poetry/bin:$PATH"

# pyspark
# export JAVA_HOME=/usr/lib/jvm/java-1.8.0-openjdk-amd64
# export SPARK_HOME=/home/patrick/apache-spark/spark-2.4.1-bin-hadoop2.7
# export PATH=$PATH:$JAVA_HOME/bin:$SPARK_HOME/bin
# export PYSPARK_PYTHON=python3

# ruby
# export PATH="$HOME/.rbenv/bin:$PATH"
# eval "$(rbenv init - zsh)"

# export PATH="$PATH:/opt/mssql-tools/bin"

# my aliases 
alias ll='ls -alhF'
alias la='ls -A'
alias l='ls -CF'
alias upfullclean='sudo apt-get update -y && sudo apt-get upgrade -y && sudo apt-get autoremove -y'
alias upfull='sudo apt-get update -y && sudo apt-get upgrade -y'
alias up10k='git -C ${ZSH_CUSTOM:-$HOME/.oh-my-zsh/custom}/themes/powerlevel10k pull'
alias upall='printf "### Updating System ###\n\n" && upfullclean && printf "\n\n### Updating OhMyZSH ###\n\n" && omz update && printf "\n\n### Updating Powerlevel 10k ###\n\n" && up10k'

alias avenv='source .venv/bin/activate'
alias dvenv='deactivate'

alias weather='curl wttr.in/Braunschweig'
alias howdoi='~/tools/.venv/bin/howdoi'

alias chrome='/mnt/c/Program\ Files\ \(x86\)/Google/Chrome/Application/chrome.exe'
alias winc='cd /mnt/c/'
alias weather='curl wttr.in/Braunschweig'
